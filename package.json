{
  "name": "farmers-boot-retry",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:unit": "node ./functions/api/operations/test_apply_treatment.js",
    "test:integration": "node ./functions/api/operations/test_integration.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "deploy": "./deploy.sh",
    "deploy:windows": "powershell -ExecutionPolicy Bypass -File ./deploy.ps1",
    "build": "cd frontend && npm run build",
    "build:ci": "cd frontend && VITE_SUPABASE_URL=https://dummy.supabase.co VITE_SUPABASE_ANON_KEY=dummy_key VITE_MAPBOX_TOKEN=dummy_token npm run build",
    "dev": "cd frontend && npm run dev",
    "dev:local": "wrangler pages dev frontend --local --port 8788 --env local",
    "dev:functions": "wrangler dev --local --port 8787 --env local",
    "setup:local": "echo 'Setting up local development environment...' && echo '1. Install Supabase CLI: npm install -g supabase' && echo '2. Start Supabase locally: supabase start' && echo '3. Copy .env.example to .env and fill in local values' && echo '4. Run migrations: supabase db reset' && echo '5. Run: npm run dev:local'",
    "setup:local:windows": "powershell -ExecutionPolicy Bypass -File ./setup-local.ps1",
    "setup:local:unix": "bash ./setup-local.sh"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "dotenv": "^16.0.0",
    "@sentry/cloudflare": "^8.0.0"
  }
}
